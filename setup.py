#!/usr/bin/env/python
from setuptools import setup

setup(
    name = 'planets',
    version = '1.0',
    description = 'A pygame based planetary model generator.',
    packages = ['planets'],
    include_package_data = True,
)
